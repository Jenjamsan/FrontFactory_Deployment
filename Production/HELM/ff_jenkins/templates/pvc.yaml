kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: ff-jenkins-pvc
  namespace: ff-production
  annotations:
    volume.beta.kubernetes.io/storage-class: azure-disk
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 6Gi