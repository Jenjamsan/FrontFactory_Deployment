apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ff-preprod-api-generation-pvc
  namespace: ff-qualification
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi