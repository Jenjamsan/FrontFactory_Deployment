apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ff-preprod-keycloak-pvc
  namespace: ff-qualification
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi