apiVersion: v1
kind: Service
metadata:
  name: {{ include "ff_keycloak.fullname" . }}
  labels:
    {{- include "ff_keycloak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ff_keycloak.selectorLabels" . | nindent 4 }}
