apiVersion: v1
kind: Service
metadata:
  name: {{ include "ff_delivery.fullname" . }}
  labels:
    {{- include "ff_delivery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ff_delivery.selectorLabels" . | nindent 4 }}
