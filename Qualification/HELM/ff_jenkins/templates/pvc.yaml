apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ff-jenkins-pvc
  namespace: ff-qualification
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-1
  resources:
    requests:
      storage: 5Gi
