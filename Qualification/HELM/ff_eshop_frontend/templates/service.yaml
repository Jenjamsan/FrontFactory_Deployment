apiVersion: v1
kind: Service
metadata:
  name: {{ include "ff_eshop_frontend.fullname" . }}
  labels:
    {{- include "ff_eshop_frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ff_eshop_frontend.selectorLabels" . | nindent 4 }}
