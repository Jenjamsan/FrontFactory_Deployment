apiVersion: v1
kind: Service
metadata:
  name: {{ include "ff_parsing_api.fullname" . }}
  labels:
    {{- include "ff_parsing_api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ff_parsing_api.selectorLabels" . | nindent 4 }}
